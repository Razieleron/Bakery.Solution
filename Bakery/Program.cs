using System;
using Products.Models;

namespace Bakery
{
  class Program
  {
    static void Main()
    {
      // ScrabbleScore scrabbler = new ScrabbleScore();
      // Console.WriteLine("Hey there gimme a word");
      // string input = Console.ReadLine().ToLower();
      // Console.WriteLine(ScrabbleScore.callScore(input)); 
    }
  }
}